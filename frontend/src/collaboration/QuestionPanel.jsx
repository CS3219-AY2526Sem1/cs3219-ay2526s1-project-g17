export default function QuestionPanel({ sessionId }) {
    return (
        <div style={{ padding: '15px', borderBottom: '1px solid #ccc', height: '100%' }}>
            <h3>Problem Statement</h3>
            <p>Implement a function that reverses a given string.</p>
            {/* Real implementation would fetch the question based on sessionId */}
        </div>
    );
}